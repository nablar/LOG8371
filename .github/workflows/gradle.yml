name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Grant execute permission for gradlew
      run: chmod +x ../../gradle-5.0/bin/gradle
    - name: Build with Gradle
      run: ../../gradle-5.0/bin/gradle :modules:ingest-geoip:test:src:test --tests org.elasticsearch.ingest.geoip.GeoIpProcessorFactoryTests 
